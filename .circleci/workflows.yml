version: 2.1

orbs:
  docker: circleci/docker@1.5.0

parameters:
  app1:
    type: boolean
    default: false
  app2:
    type: boolean
    default: false
  app3:
    type: boolean
    default: false

jobs:
  app1-job:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - when:
          condition: <<pipeline.parameters.app1>>
          steps:
            - checkout
            - run: echo "running"
      - unless:
          condition: <<pipeline.parameters.app1>>
          steps:
            - checkout
            - run: echo "not running"

  app2-job:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - when:
          condition: <<pipeline.parameters.app2>>
          steps:
            - checkout
            - run: echo "running"
      - unless:
          condition: <<pipeline.parameters.app2>>
          steps:
            - checkout
            - run: echo "not running"

  app3-job:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - when:
          condition: <<pipeline.parameters.app3>>
          steps:
            - checkout
            - run: echo "running"
      - unless:
          condition: <<ppipeline.parameters.app3>>
          steps:
            - checkout
            - run: echo "not running"

workflows:
  app1:
    jobs:
      - app1-job:

  app2:
    jobs:
      - app2-job:

  app3:
    jobs:
      - app3-job: