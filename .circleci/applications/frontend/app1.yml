version: 2.1

parameters:
  app1-fe:
    type: boolean
    default: false

executors:
  base:
    docker:
      - image: cimg/base:stable
    resource_class: small

jobs:

  tests-done:
    executor: base
    steps:
      - run: echo "DONE"

  build-app1-fe:
    executor: base
    steps:
      - run: echo "DONE"

workflows:
  app1-fe:
    when: << pipeline.parameters.app1-fe >>
    jobs:
      - build-app1-fe
      - tests-done:
          requires:
            - build-app1-fe